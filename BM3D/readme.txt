The Cmake folder holds the Cmake.list file, which can be compiled using the Cmake tool to generate executables.

The build file has the solution for Visual studio version 2022.

Both folders are for the same version of the BM3D algorithm C++ implementation. In order to run this application, the OpenCV 4.6 image processing library needs to be installed.